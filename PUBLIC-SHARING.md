# 本机临时公网试用

使用 Cloudflare Quick Tunnel 将公网 HTTPS 请求转发到本机 `127.0.0.1:3004` 密码网关，再连接现有 `127.0.0.1:3001` 工作台。不需要 Oracle 账号或云服务器，运行时仍依赖此电脑联网开机。

应用使用独立账号，普通用户只能访问自己的数据，原有本机数据归管理员账号。公共看板仅展示管理员主动发布的统计快照。旧的共享 trial 密码不再用于应用登录。

网关脚本：`scripts/share-local-proxy.mjs`。当前公网地址和进程信息只存于被 Git 忽略的 `.local-run/public-share/`；`access.json` 仅文件所有者可读。启用 accountMode 后，私有 API/媒体由应用账号会话验证，登录、注册及公开看板可匿名访问；旧 Basic 模式仍可作为可选外层保护。HTTPS 由公网通道提供，本机网关仅监听回环地址。网关校验公网 Origin 后转换为本机来源，不放行其他网站来源；旧样例分析接口不开放。

公网单请求上限为 90 MiB，网页改用每片 8 MiB 的分片上传，完整视频上限为 500 MiB，已验证 104 MiB 文件组装。网关支持流式转发与视频 Range 请求，不会将整个视频缓存到内存。

关闭公网分享（本机工作台继续运行）：

```bash
python3 scripts/stop-public-share.py
```

连接重启后临时域名可能改变，需更新 `access.json` 的 `origin` 并重新分享。随机新域名可能遇到 DNS 负缓存，需验证公共 DNS 和普通浏览器网络均可访问后再分发。不要将密码或带凭据的 URL 提交到 Git。

网关验证：`node --test scripts/share-local-proxy.test.mjs`，覆盖匿名/错误密码拒绝、跨站拒绝、凭据不转发、上传请求体透传、Range、超限拒绝。公网验证应包括页面鉴权、视频及集锦分段读取、上传来源校验。已有视频识别质量限制仍适用。
