# 示例视频自动得分实验

> 历史实验记录：当前首页已替换为任意视频上传、后台分析与跨视频累计工作台，见 [WORKBENCH.md](WORKBENCH.md)。下文描述旧的固定示例实验，不代表当前首页或新上传视频的处理流程。

首页 `http://localhost:3001` 直接展示自动个人得分表。事件检测、归属、排除与汇总由本地 Python 处理，不要求用户逐球录入。原有人工记分工具折叠在下面，其浏览器记录保持独立。

这是固定机位示例的实验结果，并未达到完整比赛统计的准确度。球员 P01–P11 的初始外观模板由此前人工整理；本次事件归属自动计算。其他视频尚未接入此管线。

## 处理方法

1. 全片 10 fps 篮球检测，另对篮网运动窗口放大检测。
2. 篮网出口连续向下的球轨迹确认候选进球；单帧球检测仅在下一帧模板匹配显示明显向下位移时补充。篮网运动本身不计分。
3. 投篮前的篮球和手腕位置关联；缺失离手帧时，用连续双手投篮姿势作较弱的估计。姿态模型在 CPU 上运行，避开当前版本的 MPS 姿态警告。
4. 以肩、髋、膝、踝对齐衣着、裤子和鞋子的外观描述，匹配初始球员模板；竞争身份不明确则不计入。
5. 运动人数和出手周围人数用于排除疑似非比赛投篮。这不是可靠的比赛状态识别器，会错排空位进球、漏排死球投篮。
6. 目前仅明确油漆区内的脚部位置计 2 分，其余为 2–3 分区间。三分线尚未完成标定。不能将区间下界当作最终得分。

## 本地运行

已有本地模型、Python 环境、正向 10 fps 代理视频和外观模板时：

```sh
./scripts/run-auto-score.sh
```

重复验证归属（复用已检测的球和姿态缓存）：

```sh
.venv-analysis/bin/python scripts/auto-score-sample.py --detections .local-run/auto-score/combined-detection.json
```

主要输出为 `.local-run/auto-score/automatic-scores.json`，API 为 `/api/automatic-scores`。页面可导出包含截图的 JSON。原视频 SHA-256、代理视频 SHA-256 用来核对检测来源；初始球员模板亦须与视频一致。缓存与输出不提交 Git。

模型：Ultralytics 官方 YOLO11s Pose；篮球检测使用 avishah3/AI-Basketball-Shot-Detection-Tracker 的 best.pt（受限反序列化加载）。这些模型在本视频上的精度未经过标注集评测，不能引用其他数据集精度作为本应用准确率。

## 验证与范围

` .venv-analysis/bin/python scripts/test-auto-score.py` 验证下穿计数、重复去除、侧向落球不计分、静止和上升球不计分、未知身份和排除事件不进入总分，以及分值区间汇总。规则测试通过不代表视觉准确率通过。

尚未实现：可靠比赛/热身/死球识别、稳定跨遮挡身份追踪、完整三分线标定、助攻和篮板、换人或每局分段、任意上传视频的自动处理。

## 本次运行结果

已分析示例完整 552.7 秒（5,527 帧全画面检测，另 902 帧篮筐窗口检测）。最终识别出 6 个带篮球轨迹依据的候选进球：1 个自动归属至 P01（白色上衣、红鞋，241.8 秒，分值暂为 2–3），1 个疑似非比赛投篮自动排除，4 个无法归属。仍有明显漏检，因此不能视为全场个人成绩。

Next.js 生产构建通过；6 项自动计分规则测试与 7 项原记分逻辑测试通过；已核对 API 返回、来源哈希、头像与汇总一致性，并在浏览器检查首页。上述验证只证明管线和汇总运行，视觉识别效果仍未达标。
