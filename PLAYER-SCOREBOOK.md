# 球员截图与个人统计

当前首页为辅助记分台。原始首页备份在 `.local-run/original-page.tsx`。

用户提供的真实录像可以通过首页“打开文件夹中的示例.mp4”直接打开。“载入本地初筛结果”提供球员候选截图、篮筐动作片段和热身/死球排除。具体实验范围与限制见 [SAMPLE-ANALYSIS.md](./SAMPLE-ANALYSIS.md)。

## 使用

1. 选择视频或打开示例，暂停在人物清晰的时间。
2. 点击“识别当前画面球员”。模型返回真实人物框与截图；确认候选人物后加入名单。漏检可截图后拖动框选。
3. 填写姓名、球队、号码。同一人的重复截图可以合并。检测器不能判断人物是否为参赛球员，也不能识别真实姓名。
4. 选择球员，在视频发生事件的时刻记录两分、三分、罚球、助攻、篮板、抢断、盖帽或失误。
5. 在事件列表回看、改归属、修改时间/类型或删除错误事件。未分配或未确认事件不计入统计。
6. 导出球员 CSV 或完整 JSON。记录自动保存在当前浏览器的 IndexedDB 中；重新选择相同视频可以恢复。浏览器清除站点数据会清空本地记录。

## 自动化边界

- 人物检测、截图裁剪、已确认事件的个人统计为自动处理。
- 当前不自动识别球员姓名、不进行跨帧身份跟踪，不自动判定得分/助攻/篮板。身份与比赛事件由用户确认。
- 主流程不调用旧的随机人物框或合成事件回退。旧算法代码暂保留在仓库中，不用于新记分台。
- 通用检测器可能漏掉远距离/遮挡人物，也会检测观众或裁判。截图候选不等同于正式球员名单。
- 同号码、不同队伍的球员使用不同 UUID；统计不以姓名或号码作关联键。

## 模型与验证

使用 [TensorFlow 官方 COCO-SSD](https://github.com/tensorflow/tfjs-models/tree/master/coco-ssd)，mobilenet_v2 权重本地托管。首次加载约 66 MB，之后浏览器可缓存。

重新下载模型：

```bash
python3 scripts/download-player-model.py
```

记账逻辑测试：

```bash
PATH=/opt/homebrew/opt/node@18/bin:$PATH npx tsx --test lib/scorebook.test.ts
```

完整自动统计仍需独立的球员多目标跟踪、球衣重识别、篮球/篮筐检测、球权与进球事件模型，以及针对真实比赛片段的标注评估。本次没有声称提高自动比赛事件识别准确率。
