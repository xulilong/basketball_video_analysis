# MT球员统计工作台

当前首页为真实的本地上传分析流程：上传视频 → 开始分析 → 人物截图、个人得分估算与历史累计。它不读取旧的示例识别结果，不依赖 Codex 在对话中提前分析视频。

## 本地运行

在本项目目录执行 `./start-local.sh`，访问 http://localhost:3001 。首次构建或修改页面后，使用本机 Node 18 执行 `npm run build`，然后重启服务。

分析依赖 `.venv-analysis/bin/python`、ffmpeg，以及 `.local-run/models/` 中的 `yolo11s.pt`、`yolo11s-pose.pt`、`basketball-best.pt`、`osnet-ain-msmt17.pth`。这些依赖已安装在当前电脑；只复制源码到另一台电脑不会自动安装模型。

支持 MP4/MOV/WebM/MKV，单文件最多 500 MB、时长最多 120 分钟。分析在后台运行，关闭页面不会中止任务；再次打开页面会读取进度。一个数据目录内的任务串行执行，可停止并重试。

## 识别与累计

- V2：人物约 10 fps 连续跟踪；篮筐局部按原视频帧率（最高 30 fps）检测。批量推理减少模型调用开销。补充连续运动轨迹，并检查篮网中部的通过证据或形变，避免把侧面落下的未进球直接算作命中。
- OSNet 在支持 MPS 的 Mac 上使用 GPU，并在同一个任务内复用模型；可通过 `BASKETBALL_REID_DEVICE=cpu` 回退到 CPU。
- OSNet 为每条轨迹提取多张截图特征。先组成人物档案，再执行全视频第二轮相似度合并；同一时刻位置不同的人物观测不得冲突，高度重叠的重复检测框不直接视为两个人。每轮按当前档案检查相似度，合并后的代表图和多视角图库继续参与下一轮，直到不再变化；这不等于要求跨所有轮次的每张原始截图都彼此接近。第二轮不要求第一、第二候选拉开差距，因为两者可能都是同一人的重复记录。
- 在 ReID 之前，重新检查每条跟踪编号下的截图：外观冲突时拆成独立片段，再交给多视角合并。这样不会直接丢掉被跟踪编号切换覆盖的投篮者。即便如此，遮挡和相似衣着仍会造成错误，不能保证最终人数就是实际人数。
- 投篮者同时参考多帧外观、邻近轨迹和姿态。缺少连续运动或周围比赛参与证据时，标为未确定且不加入累计；同时缺少两种证据时疑似热身/死球。此规则会漏记真实空位或慢节奏进球，仍需改进。
- 新视频的有效事件自动汇总到对应档案。根据视频文件的 SHA-256 去重，同一文件改名、重复上传、刷新页面均不会重复加分。重新编码或裁剪后的重叠片段属于不同文件，目前不能识别其内容重叠，可能重复累计。
- 累计从事件和人物关联重新计算，修改关联后历史统计同步更新，不使用反复增加的计数器。
- 不是 OCR，也不调用云端 AI 接口。运行时不上传视频或人物截图到外部服务。

## 当前质量限制（必须保留）

示例视频现已增加“三分线”预览：`scripts/calibrate-court-line.py` 在经视觉确认的区域内，用 65 帧的中值背景去除人物遮挡，再拟合实际黑色弧线。标定保存在该视频目录的 `court.json`，绑定源文件 SHA-256；黄色只标记有像素支持的可见弧段。此步骤是辅助标定，不是任意视频的全自动三分线检测，也尚未结合起跳前鞋底位置来修改事件分值。新视频不会套用此视频坐标。

这是一版识别仍待改进的实验实现，并非可靠的比赛技术统计。旧版在示例 9 分 12 秒视频中用时约 9 分钟，但存在严重的人物拆分和漏检。V2 增加了推理工作量，不能套用旧版耗时；短片测试也不能推算为完整比赛的准确率。

两分和三分尚未可靠判定。分值不明的进球现在单列“待判分”，不再按 2–3 分区间累计；`knownPoints` 为已判定分数，`unknownValue` 为待判分进球数。兼容字段 `pointsMin` / `pointsMax` 均等于 `knownPoints`。“—”不表示真实零分。助攻、篮板和自动分局尚未接入。

旧视频可点击“使用新版重新分析”。重算成功后替换同一视频结果，不创建重复视频、不重复累计；重算期间和失败后保留旧结果。旧版本的自动碎片档案在不被其他视频引用时清理，用户命名或编辑过的档案保留。重算前的记录保存在该视频目录 `revisions/`。

## 数据与代码

数据保存在 `.local-run/workbench/database.json` 和 `videos/<文件哈希>/`。备份应在暂停写入时复制整个目录，保留源视频、人物档案、结果和关联；只备份网页源码不包含统计数据。可通过 `BASKETBALL_DATA_DIR` 切换独立数据目录。

- `components/ScoringWorkbench.tsx`：上传、分析进度、结果与累计界面。
- `lib/workbench-server.ts`：流式上传、持久化、后台任务。
- `lib/workbench-domain.ts`：跨视频身份关联、事件累计和去重。
- `scripts/analyze-upload.py`：针对实际上传视频的检测与事件识别。
- `scripts/player-reid.py`：OSNet 外观关联及投篮者复核。
- `scripts/track-fragments.py`：拆分跟踪编号中的外观冲突，保留每个片段的观测与截图。
- `scripts/identity-tracks.py`：多视角轨迹归并和全视频第二轮合并。
- `scripts/rim-motion.py`：局部运动补检及穿网证据检查。
- `scripts/merge-video-players.py`：复用已有观测生成待评估的人物合并提案，不直接修改计分数据库。

开发时可使用 `scripts/analyze-upload.py --job-dir <目录> --resume-detections` 复用检测缓存。缓存须匹配代理视频 SHA-256 和 V2 格式；普通网页重算仍执行完整检测。

模型来源：[Ultralytics](https://github.com/ultralytics/ultralytics)、[篮球检测模型原项目](https://github.com/avishah3/AI-Basketball-Shot-Detection-Tracker)、[OSNet 作者权重](https://huggingface.co/kaiyangzhou/osnet)及 [Torchreid](https://github.com/KaiyangZhou/deep-person-reid)。`scripts/vendor/osnet_ain.py` 保留作者架构代码及 MIT 许可，其他模型按各自来源许可使用。

验证：`npx tsx --test lib/workbench-domain.test.ts`、`.venv-analysis/bin/python scripts/test-upload-analysis.py`、`.venv-analysis/bin/python scripts/test-identity-tracks.py`、`.venv-analysis/bin/python scripts/test-track-fragments.py`、`.venv-analysis/bin/python scripts/test-rim-motion.py`、`npm run build`。规则测试不代表视频识别准确率；实际片段和完整视频的检测结果必须单独核对。

## 2026-09-14 本轮验证记录

- 真实上传短片（示例的 235–247 秒）通过网页流程完整重算：初步整理 17 条人物记录，第二轮合并为 15 条；检测到 1 次进球，并关联到白色 6 号、红鞋球员。仍有重复档案，不能把 15 条视为准确人数；分值仍待判定。该次分析用时 137.9 秒，测试时另一条全片任务也在运行，不能直接外推处理速度。
- 同一短片的缓存调试曾得到 19 → 14 条；它与正式上传重新检测的结果不同，不应混用这两个数字，也不构成旧版整体准确率的提升比例。
- 全片快速篮筐候选检查中，11 个候选有 3 次侧面未进球，新穿网检查将这 3 次拦下。这是开发样本核对，不是全片进球召回率、比赛进球总数或独立测试集准确率。
- 7 项 TypeScript 累计逻辑测试、25 项 Python 规则测试通过；构建通过。真实重复上传未增加视频和球员记录；服务重启后，短片的 1 次个人进球保留。旧结果替换、用户命名档案保留及重复累计由领域测试覆盖。

- MPS 回归：同一批 15 张截图，CPU 约 0.57 秒，MPS 冷启动约 6.66 秒、预热后约 0.07 秒，归一化特征最大差异约 4.47e-7。相同检测缓存经 GPU 重算仍为 17 → 15 条、同一投篮者、1 次进球。缓存重算 44.4 秒不包含首次检测。

- 合并队列优化：120 组随机输入与原实现的映射、合并记录一致（含共现约束和候选间隔）；新增过期距离失效测试。相邻画面顺序读取在 9 次前进/回退取帧中与逐次精确 seek 像素一致。

- 全片中间版本：一次全局合并仍有 273 条人物记录；继续比较合并后的图库，提案降至 111 条，仍明显过多。检查发现部分轨迹混入相近颜色衣着的另一人，HSV 拆分未能完全排除这种污染。不能宣称人数问题已经解决。

- 最终部署的示例结果：111 条人物记录、14 个候选事件，2 个进球估算关联到个人且通过当前比赛状态规则；其余候选不进入累计。00:22.6 的热身/死球候选未计入。两分/三分仍未判定，没有填入确定分数。旧示例结果为 128 条记录，另一个 80 条记录的视频未在本轮重算；两者不能混作同一个视频比较。
- 最终运行结束后，仅使用同一份已保存的 `gameContext` 观测重新应用更新后的通用比赛状态规则；原结果另存备份，未手工填写球员或进球时间。新上传直接执行相同规则。示例旧档案及关联已归档到 `revisions/`，替换通过事务执行。
- 当前结论：迭代合并功能已经实现，仍然存在严重重复人物、漏记和不确定归属，未达到可靠个人得分统计的要求。规则测试与构建通过不代表视频准确率达标。

## 进球集锦页面（2026-09-14）

入口 `/highlights`，首页右上角“进球集锦 · 自动剪辑”。上传成功后自动启动本地剪辑任务，也可选择已上传的视频；支持刷新恢复进度、完整 MP4 预览/下载及逐片段下载。每个检测事件取前 5 秒、后 2 秒，重叠区间合并，输出 H.264 / AAC MP4 并保留原声。无音轨的视频保持静音。

`POST /api/videos` 复用流式上传与文件去重；`POST/GET /api/videos/:id/highlights` 启动/查询任务；`GET/HEAD /api/videos/:id/highlights/media` 提供 Range 播放及下载。`file=clip-001.mp4` 指定片段，`download=1` 设置下载响应。结果独立保存在工作台数据目录的 `highlights/videos/<sha256>/`，不修改个人得分。

分析器的 `--highlights` 分支只运行篮球/篮筐检测和进球轨迹判断，跳过人物跟踪、人物合并和投篮归属。已完成统计分析的视频可复用校验过的视频检测缓存；新视频会完整运行检测。使用与统计任务相同的本机分析锁。实际剪辑由 `scripts/highlight-export.py` 调用本地 FFmpeg 完成。

验证：生产构建通过；区间边界、去重、重叠合并测试 3 项；原统计领域测试 7 项、进球规则测试 7 项、篮网规则测试 3 项通过。示例视频复用已有检测生成 10 段、约 70 秒集锦，完整解码验证含音轨，下载和 Range 响应通过。独立上传的 10 秒真实视频无检测缓存，约 16.6 秒完成检测和导出，生成一段 7 秒视频。这是小样本流程验证，不能推算任意视频耗时或进球准确率。

限制：仍使用现有实验性进球检测，可能漏剪/误剪；没有可靠的热身/死球识别，所以集锦可能包含这些投篮，不能作为有效比赛得分依据。无符合条件的进球时显示空结果，不生成虚假的导出文件。单机位遮挡、多篮筐及移动镜头仍可能影响结果。

补充验证：无篮筐测试视频返回空结果且下载接口返回 404；静音源可正常导出；越界 Range 返回 416，非结果文件不可下载。修正了 Next 将请求 URL 规范化为 localhost 时导致 127.0.0.1 页面上传被误拒绝的问题；按实际 Host 校验 Origin，仍拒绝外站请求，3 项回归测试通过。

## 产品页面与导航（2026-09-14）

- `/` 首页：品牌介绍、两个工作区入口、真实视频/已分析视频/进球片段数量，以及最近五段视频。概览来自 `/api/overview`，未加载时显示占位，不填充演示数据。
- `/statistics` 技术统计：保留上传、启动/停止/重分析、人物关联、姓名编辑、合并及数据导出。新增姓名搜索、仅看有进球记录、每页 12 条分页；人物关联表单展开时才加载，避免大量下拉选项拖慢页面。球场标线参考默认折叠。
- `/highlights` 进球剪辑：素材/默认设置/提示在左，预览/进度/片段/导出在右。支持拖放上传、已有视频选择、空结果、重试和连接异常提示。
- 两个工作区均支持 `?video=<sha256>`，首页最近视频可以直接进入对应工作区。剪辑页选择新视频时同步地址，刷新后仍打开所选视频。
- 共享 `ProductShell`：桌面侧边栏、窄屏顶部导航、当前页面标识、使用指南及品牌页脚；统一深蓝灰、暖橙和浅灰画布，使用独立 SVG 品牌标识。
- 视觉验证：实际浏览器桌面截图与 390px 窄屏 iframe 渲染检查，三个页面排版正常；统计搜索空结果、累计分页、导航切换、剪辑已有结果预览已实测。此次是产品框架与交互完善，模型识别准确率仍受前述限制。

## 可配置剪辑与配乐（2026-09-14）

进球剪辑支持前置 1–15 秒、后置 1–10 秒、重叠片段合并开关、现场原声和背景音乐各 0–100% 音量。默认仍是前 5 秒、后 2 秒、合并、原声 100%、不配乐。选择配置后点击“应用配置，重新生成集锦”；首次上传按当前设置处理。

提供两首本地合成的原创器乐循环（`scripts/create-preset-music.py`，无外部录音素材）：上场 / 动感节拍、落日球场 / 轻松律动。自定义音乐通过 `/api/highlight-music` 上传，支持 MP3/M4A/WAV/AAC/OGG/FLAC，最大 30 MB，转码为纯音频 AAC，最多保留前 10 分钟。音乐库支持试听，文件保存在本机。

音乐自动循环并淡入淡出；完整集锦的音乐在视频切点处保持连续；单独导出的片段从音乐开头配乐。原声与音乐独立控制，限幅防止混音溢出。无原声音轨的视频也能加配乐。

`options.json` 保存本视频设置；`events.json` 独立保存进球位置。重新生成走 `--render-only`，不再加载检测模型。每次输出写入 `exports/<generation>/`，全部完成后原子更新 `result.json`，处理中可预览旧版本，失败不覆盖上一版成片。媒体地址附版本参数使新成片立即刷新。

验证：配置校验 2 项、剪辑范围测试 4 项通过；真实 FFmpeg 集成测试验证静音、音量比例、短音乐循环、无音轨配乐及原声混音；自定义音乐上传、读取和无效上传拒绝通过。已有 10 秒视频复用事件改为 4 秒配乐集锦，用时约 1 秒；这是短片实测，不代表任意长度耗时。

## 球员档案（2026-09-14）

新增 `/players`，共享侧边导航。正式球员、视频自动识别的待关联人物、已归档档案分开管理；支持姓名/号码搜索、分页、新增、编辑、归档恢复、多张参考照片上传、设置封面和移除。每人最多 8 张照片，每张最大 10 MB，支持 JPEG/PNG/WebP，校正 EXIF 方向、移除图片元数据并缩放至最长边 1200 像素。

读取用户提供的飞书 `NQgVsX4JfhqxPbtLfGDcvjjtnqb`，Sheet `bb8701` 的 A1:T200，完整确认第 2–31 行共 30 位球员，仅导入姓名和号码，不导入尺码或付款信息；原在线表格只读。存在重复号码，号码不作为身份唯一键，0/00 保留为文本。导入以来源记录键去重，合并档案时转移来源键，避免重复导入重新产生档案。导入前备份本地数据库。

正式档案沿用统计系统的 Person ID；人物关联或合并后重新计算历史统计。归档不删除历史数据，归档档案不参与新的自动外观匹配。参考照片单独管理，不作为现有 OSNet 衣着向量输入。技术统计的关联列表优先显示正式名单，并展示球衣号码。

人脸匹配：本次完成姓名、号码及多张参考照片的资料管理，为后续接入人脸检测/特征提取/视频匹配提供数据基础。尚未接入人脸识别模型，上传照片不会立即启用人脸匹配，界面有明确说明。

验证：4 项档案领域测试和原有 7 项统计领域测试通过；HTTP 实测新增、编辑、归档、照片上传/读取/移除、无效图片拒绝通过；浏览器检查 30 位正式球员的真实列表及档案界面。名单导入工具：`scripts/import-player-roster.ts <roster.json>`，数据文件位于忽略版本控制的 `.local-run/`。
