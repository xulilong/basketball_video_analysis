# 私有工作空间与管理员发布

普通使用者注册独立账号，视频、人物档案、照片、音乐、上传分片和统计存储在各自目录。服务端为所有私有接口（包括媒体 Range/HEAD）验证 HttpOnly 会话，并通过 AsyncLocalStorage 选择账号目录；不依赖前端筛选。相同视频哈希在不同账号中也不会相互暴露。账号密码使用加盐 scrypt 哈希，会话仅保存 token 的 SHA-256，7 天过期。

管理员由 `node scripts/init-admin.mjs` 初始化，原有 `.local-run/workbench/database.json` 及媒体保留在管理员私有空间。初始化密码写入 `access/admin-initial.json`，该文件权限为 600，位于被 Git 忽略的数据目录中。新用户注册无法指定管理员角色。

管理员在 `/admin` 选择账号和球员，将该档案统计直接发布到 `/board`，不需要使用者先提交。每次发布从服务端重新读取统计，忽略客户端传来的计数；同一账号同一球员重复发布更新原快照，不叠加。撤回后不再公开。公开字段仅为姓名、号码、视频数、进球数、已判定得分、待判分进球数、发布时间，不公开账号、照片、视频或外观向量。

快照不会随用户私有统计改变自动更新，需要管理员再次同步。同名档案或不同账号中的同一球员不会自动合并，避免仅凭姓名把不同人的分数混在一起。管理员此版本可查看和发布各账号的统计，未提供切换进入他人视频工作区的界面。

## 大视频上传

网页把视频分成 8 MiB 分片，逐片 SHA-256 校验及失败重试，最后在服务器流式组装、再次计算完整文件哈希并按账号去重。完整视频最大 500 MiB，公网单请求不超过原有 90 MiB 网关限制。每个账号最多同时有两个未完成上传；取消会清理分片，超过 24 小时的遗留分片在该账号下一次创建上传时清理。当前支持单次上传中的重试，不支持关闭浏览器后的断点续传。

## 视频只保留在浏览器

处理完成后，使用者进入 `/library` 点击“保存到浏览器并清理视频”：

1. 服务器生成当前原视频、完整集锦及单独片段的清单和校验值。
2. 浏览器下载文件并写入 IndexedDB，再读回校验。
3. 确认写入完成，提交当前保存凭据；服务器检查结果没有变化、无处理任务运行，再清理该视频的原文件、处理缓存、导出集锦。
4. 统计、球员档案及发布快照保留，继续支持累计和管理员发布。

下载、容量或校验失败不会启动服务器清理。清理中断可重试；过期清理凭据不能删除重新上传后的文件。浏览器可播放、下载原视频和集锦，也能通过分片上传恢复源视频用于重新分析。未点击保存并清理的文件仍在服务器，没有静默自动删除历史资料。

浏览器数据绑定账号及当前网站来源，只在保存它的浏览器里可见。清理浏览器网站数据、换浏览器、换域名都可能使其丢失或无法访问；临时公网地址更换后，新网址不能直接读取旧网址的 IndexedDB。重要视频应下载到设备；本次不提供跨网址自动迁移或云备份。大文件本地保存受浏览器磁盘配额与内存限制，保存失败会保留服务器副本。

## 验证

- 19 项账号/统计/来源/剪辑配置测试通过，含私有目录并发隔离、越权请求拒绝、管理员发布去重、公开字段限制、分片校验、保存凭据及恢复后旧凭据拒绝。
- HTTP 验证独立账号、匿名拒绝、104 MiB 分片组装和视频 Range。
- 浏览器验证登录、账号资料、真实短片保存到 IndexedDB、清理服务器源文件/中间文件、本地 Blob 播放、恢复视频。
- 识别模型本身的准确率限制不因账号和存储改造而改变。
