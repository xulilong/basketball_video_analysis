# Domain (or subdomain) with DNS pointing to the server; no scheme/path.
SITE_HOST=basketball.example.com
# Run: docker run --rm -it caddy:2 caddy hash-password
# Paste the resulting bcrypt hash inside SINGLE quotes to preserve dollar signs.
# Do not publish this file after adding real settings.
TRIAL_PASSWORD_HASH=''
